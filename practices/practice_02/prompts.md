# Журнал экспериментов Практики 2

- **Выбранный слабый артефакт Практики 1:** `practices/practice_01/context.md` — Context Pack.
- **Что в нём нужно улучшить:**
  1. Правила (`SEC-1`…`OBS-1`) перечислены без ссылок на источник в `CASE.md` — проверить их можно только вручную.
  2. Нет обоснования, почему вошли именно эти 7 правил, а `snake_case` и i18n исключены — решение выглядит произвольным.
  3. Один пример хорошего комментария; непонятно, как выглядит «правильный» риск в других файлах (`api.py`, `tests_*.md`).
  4. Формат риска упомянут как `{file, line, evidence, risk}`, но не показан на примере JSON-ответа.
- **Как поймём, что изменение полезно:** другой инженер откроет `context.md`, сверит каждое правило с `CASE.md` по строке и не задаст уточняющих вопросов.

| Техника | Файл эксперимента | Изменённый файл Практики 1 | Конкретное изменение | Проверка | Что отклонили |
|---|---|---|---|---|---|
| Few-shot | [`few_shot/experiment.md`](few_shot/experiment.md) | `practices/practice_01/context.md`, раздел «Хорошие примеры» | Один пример расширен до трёх — добавлены `OUT-1` (`review_service.py:12`) и `API-1` (`api.py:11`) | Сверили строки 11 и 12 в `TRAINING_PR.diff` | Абстрактный пример без `file:line` | 
| R.C.T.F. | [`rctf/experiment.md`](rctf/experiment.md) | `practices/practice_01/prompts.md`, раздел «Master Prompt v1», подразделы 1–4 | Подразделы 1–4 переписаны в формате Role / Context / Task / Format с полным текстом запроса | Прогнали запрос на `TRAINING_PR.diff` — 3 риска с `file:line`, совпадают с P1-02 | Предложение усилить роль «senior security expert» — не относится к правилам репозитория |
| Chain of Verification | [`chain_of_verification/experiment.md`](chain_of_verification/experiment.md) | `practices/practice_01/tests_unit.md`, таблица проверок | Удалены 2 строки без подтверждения: `test_strip_secrets_keeps_plain_code` и `test_no_github_actions` | Прогнали CoV-вопросы к каждой строке; осталось 10 проверок, каждая подтверждена правилом или строкой diff | Предложение модели добавить новые проверки на `OBS-1` и `QA-1` — они уже есть в таблице |
| Tree of Thoughts | [`tree_of_thoughts/experiment.md`](tree_of_thoughts/experiment.md) | `practices/practice_01/adr.md`, раздел «Рассмотренные альтернативы» | Плоский список из 5 альтернатив заменён на два ToT-выбора: реализация анализа (4 варианта) и реализация `SEC-1` (3 варианта A/B/C). Зафиксирован итог: A+B принято, C отклонено | Сверили критерии с `SEC-1` из `CASE.md`; выбранная альтернатива проверяется тестом `test_strip_secrets_before_llm` | Вариант C (allow-list) — гарантирует `SEC-1`, но снижает качество ревью |
| RAG | [`rag/experiment.md`](rag/experiment.md) | `practices/practice_01/context.md`, подраздел «Факты и правила» | К каждому из 7 правил добавлена ссылка на источник (`CASE.md`, раздел «Правила репозитория») и фраза «Почему входит» с привязкой к `TRAINING_PR.diff`. Исключённые правила (`snake_case`, i18n) — со ссылкой на раздел «Эти правила не относятся к учебному PR» | Сверили построчно с `CASE.md`; все 7 правил присутствуют в источнике без изменений | Правило «все коммиты в Conventional Commits» — в `CASE.md` такого нет, отклонили как выдуманное |
| ReAct | [`react/experiment.md`](react/experiment.md) | `practices/practice_01/tests_e2e.md`, строка «Отказ LLM по таймауту» | В колонке «Наблюдаемый результат» добавлен явный критерий проверки: ответ ≤ 11 с, код 504 или контролируемый JSON с `summary` и `checks`, без сырого исключения | Прогнали ReAct-цикл из 5 шагов; на шаге 2 обнаружили, что `REL-1` ещё не реализован, поэтому критерий сформулирован так, чтобы работать до и после доработки | Замена сценария на «проверить, что код не делает вызовов без timeout» — это дублирует unit-тест `test_llm_timeout` |

## Независимое ревью

Red-team по чек-листу из `README.md` проведён самостоятельно: проверяющий получил спецификацию (только тексты изменённых артефактов, без запросов и контекста экспериментов) и нашёл три замечания.

| Замечание другой команды | Где исправили | Evidence |
|---|---|---|
| **Двусмысленность.** В `context.md` в разделе «Формат входа и результата» написано «Markdown-отчёт с разделами», но не указано, где именно в отчёте лежат поля `file`, `line`, `evidence`, `risk` — в тексте раздела или внутри JSON-блока. | `practices/practice_01/context.md`, подраздел «Формат входа и результата» | Формулировка уточнена: «Markdown с разделом `## Риски`, внутри которого ≤ 3 элемента, каждый — в формате `file:line — нарушение (правило). Evidence. Проверка.`» |
| **Непроверяемое требование.** В `adr.md` после ToT-правки осталась фраза «Помощник выдаёт правдоподобный, но неверный риск, и ревьюер ему доверяет» как главный риск, но нет способа проверить, что ревьюер не доверяет бездумно. | `practices/practice_01/adr.md`, раздел «Последствия и главный риск» | К главному риску добавлена проверка: «выборочная ручная сверка 10 отчётов в спринт; доля подтверждённых рисков ≥ 40%; при падении ниже — пересмотр промпта» |
| **Пропущенный источник.** В `context.md` в разделе «Что пока неизвестно» указан SLA, но нет ссылки на то, откуда взялось ограничение 15 секунд (или 10 секунд по `REL-1`). | `practices/practice_01/context.md`, подраздел «Что пока неизвестно» | Добавлена ссылка: «Ограничение 10 с — из правила `REL-1` в `CASE.md`, раздел «Правила репозитория». Точный SLA продукта в целом — вопрос к заказчику» |